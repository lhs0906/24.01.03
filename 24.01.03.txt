24.01.03

html 공부

- h(n) 태그

1. <h1> 태그는 한 번만 사용한다.
2. <h1> 태그부터 순차적으로 사용해야 한다.

- p, 단락 태그 
: 1. 웹 브라우저의 가로 한 줄을 사용하는 태그 (유동적)
  2. 공백은 웹 브라우저에서는 최대 한 칸으로 표시됨.
     -> &nbsp; 코드를 사용해서 원하는 만큼의 스페이스 공백을 만들 수 있음
          (&nbsp; 는 html의 Entity 코드이다.)

- br, 줄 바꿈 태그